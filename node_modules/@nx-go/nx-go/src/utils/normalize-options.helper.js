"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizeOptions = void 0;
const devkit_1 = require("@nrwl/devkit");
const should_use_go_work_1 = require("./should-use-go.work");
function normalizeOptions(tree, projectBase, options) {
    const nxJson = (0, devkit_1.readJson)(tree, 'nx.json');
    const name = (0, devkit_1.names)(options.name).fileName;
    const projectDirectory = options.directory ? `${(0, devkit_1.names)(options.directory).fileName}/${name}` : name;
    const projectName = projectDirectory.replace(new RegExp('/', 'g'), '-');
    const projectRoot = `${projectBase}/${projectDirectory}`;
    const parsedTags = options.tags ? options.tags.split(',').map((s) => s.trim()) : [];
    const useGoWork = options.skipVersionCheck ? !!options.useGoWork : (0, should_use_go_work_1.shouldUseGoWork)(tree, !!options.useGoWork);
    return Object.assign(Object.assign({}, options), { projectName,
        projectRoot,
        projectDirectory,
        parsedTags, skipGoMod: !!options.skipGoMod, npmScope: nxJson.npmScope, useGoWork, skipVersionCheck: !!options.skipVersionCheck });
}
exports.normalizeOptions = normalizeOptions;
//# sourceMappingURL=normalize-options.helper.js.map