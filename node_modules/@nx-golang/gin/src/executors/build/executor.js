"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const path = require("path");
const util_1 = require("../../util");
function runExecutor(options, context) {
    return tslib_1.__awaiter(this, void 0, void 0, function* () {
        const mainFile = `${options.main}`;
        const output = `-o ${options.outputPath}${process.platform === 'win32' ? '.exe' : ''}`;
        const projectName = context.projectName;
        const sourceRoot = context.workspace.projects[projectName].sourceRoot;
        // get deps
        (0, util_1.runGoCommand)(context, 'mod', ['tidy'], {
            cwd: path.join(context.cwd, sourceRoot),
        });
        (0, util_1.runGoCommand)(context, 'mod', ['vendor'], {
            cwd: path.join(context.cwd, sourceRoot),
        });
        return (0, util_1.runGoCommand)(context, 'build', [output, mainFile]);
    });
}
exports.default = runExecutor;
//# sourceMappingURL=executor.js.map